# reserved keys
settings:
  inherit: true
environments:
  test:
    import:
      - "**/*.test-import.yaml"
    list:
      - 3
      - 4
import:
  - imported.yaml
  - "**/*.import.yaml"

# Source values
source_value: "source"

test_value: "source"
list:
  - 1
  - 2
col:
  a:
    - b: "source"
